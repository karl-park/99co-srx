package sg.searchhouse.agentconnect.constant

/**
 * Cross-activity extra keys
 *
 * Extra keys were defined within activities by default, but in case the keys need to be shared,
 * then put them here
 */
object ExtraKey {
    const val EXTRA_PROJECT_PROPERTY_PURPOSE = "EXTRA_PROJECT_PROPERTY_PURPOSE"
}