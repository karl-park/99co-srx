<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- pressed state -->
    <item android:color="@color/white_invertible" android:state_pressed="true" />
    <!-- selected state -->
    <item android:color="@color/white_invertible" android:state_selected="true" />
    <!-- normal states -->
    <item android:color="@color/black_invertible" android:state_pressed="false" />
</selector>
